import Script from "next/script";export default function RootLayout({
  children


}: {children: React.ReactNode;}) {
  // Function with no return statement - what happens?
  const config = {
    theme: 'dark',
    locale: 'en'
  };
}