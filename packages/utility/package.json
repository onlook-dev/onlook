{
  "name": "@onlook/utility",
  "main": "./src/index.ts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint --fix .",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit"
  }
}
