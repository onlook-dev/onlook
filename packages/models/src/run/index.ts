export enum RunState {
    STOPPED = 'stopped',
    SETTING_UP = 'setting-up',
    RUNNING = 'running',
    STOPPING = 'stopping',
    ERROR = 'error',
    TERMINATE_BATCH = 'Terminate batch job (Y/N)? ',
}
