export enum MouseAction {
    MOVE = 'move',
    CLICK = 'click',
    DOUBLE_CLICK = 'double-click',
}

export enum InsertPos {
    BEFORE = 'before',
    AFTER = 'after',
    PREPEND = 'prepend',
    APPEND = 'append',
    INDEX = 'index',
}
