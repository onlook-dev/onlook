export enum MouseAction {
    MOVE = 'move',
    CLICK = 'click',
}

export enum InsertPos {
    BEFORE = 'before',
    AFTER = 'after',
    PREPEND = 'prepend',
    APPEND = 'append',
    INDEX = 'index',
}

export enum IDE {
    VS_CODE = 'VSCode',
    CURSOR = 'Cursor',
}
