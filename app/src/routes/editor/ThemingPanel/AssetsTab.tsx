import { observer } from 'mobx-react-lite';

const AssetsTab = observer(() => {
    return (
        <div className="w-full">
            <div className="w-full pt-96 text-center opacity-70">ASSETS</div>
        </div>
    );
});

export default AssetsTab;
