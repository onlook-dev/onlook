name: onlook

services:
  web-client:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - apps/web/client/.env
    ports:
      - "3000:3000"
    restart: unless-stopped
    network_mode: host
    # networks:
    #   - default
    #   - supabase_network_onlook-backend

networks:
  supabase_network_onlook-backend:
    external: true
