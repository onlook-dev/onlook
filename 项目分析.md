# Onlook 项目详细分析

## 项目概述

**Onlook** 是一个开源的、视觉优先的代码编辑器，定位为"设计师的 Cursor"。它允许用户通过 AI 在 Next.js + TailwindCSS 中创建网站、原型和设计，直接在浏览器 DOM 中使用可视化编辑器进行编辑，实时设计与代码同步。

- **官网**: https://onlook.com
- **开源协议**: Apache 2.0
- **仓库**: https://github.com/onlook-dev/onlook
- **定位**: Bolt.new、Lovable、V0、Replit Agent、Figma Make、Webflow 的开源替代品

## 技术栈

### 核心技术
- **包管理器**: Bun 1.3.1（独占，不使用 npm/yarn/pnpm）
- **前端框架**: Next.js 16.0.7 (App Router)
- **UI 框架**: React 19.2.0
- **样式方案**: TailwindCSS 4.0.15
- **状态管理**: MobX + mobx-react-lite
- **API 层**: tRPC 11.0.0 + Zod 4.1.3
- **数据库 ORM**: Drizzle ORM 0.44.5
- **后端服务**: Supabase
- **国际化**: next-intl 4.0.2
- **AI 集成**: Vercel AI SDK 5.0.26

### 开发工具
- **TypeScript**: 5.5.4
- **测试框架**: Vitest 4.0.7 + Playwright
- **代码质量**: ESLint 9.0.0
- **UI 组件库**: Storybook 10.0.4
- **视觉回归测试**: Chromatic

## 项目架构

### Monorepo 结构

项目采用 Bun Workspaces 管理的 Monorepo 架构：

```
onlook/
├── apps/                    # 应用程序
│   ├── backend/            # Supabase 后端服务
│   └── web/                # Web 应用
│       ├── client/         # Next.js 主应用（核心）
│       ├── server/         # Web 服务器
│       └── preload/        # 预加载脚本
├── packages/               # 共享包（20个）
│   ├── ai/                # AI 功能集成
│   ├── code-provider/     # 代码沙箱提供者
│   ├── constants/         # 常量定义
│   ├── db/                # Drizzle 数据库 schema
│   ├── email/             # 邮件服务
│   ├── file-system/       # 文件系统操作
│   ├── fonts/             # 字体管理
│   ├── git/               # Git 集成
│   ├── github/            # GitHub 集成
│   ├── growth/            # 增长分析
│   ├── image-server/      # 图片服务
│   ├── models/            # 数据模型
│   ├── parser/            # JSX/TSX 解析器
│   ├── penpal/            # iframe 通信
│   ├── rpc/               # RPC 通信
│   ├── scripts/           # 脚本工具
│   ├── stripe/            # Stripe 支付集成
│   ├── types/             # TypeScript 类型定义
│   ├── ui/                # UI 组件库
│   └── utility/           # 工具函数
├── tooling/               # 工具链配置
└── docs/                  # 文档站点
```

### 核心应用：apps/web/client

这是项目的主要应用，代码量约 **73,333 行**（TypeScript/TSX）。

#### 目录结构

```
apps/web/client/src/
├── app/                           # Next.js App Router 页面
│   ├── _components/              # 全局共享组件
│   ├── about/                    # 关于页面
│   ├── api/                      # API 路由
│   ├── auth/                     # 认证相关
│   ├── callback/                 # OAuth 回调
│   ├── faq/                      # FAQ 页面
│   ├── features/                 # 功能展示页面
│   ├── invitation/               # 邀请功能
│   ├── login/                    # 登录页面
│   ├── pricing/                  # 定价页面
│   ├── privacy-policy/           # 隐私政策
│   ├── project/[id]/             # 项目编辑器（核心功能）
│   ├── projects/                 # 项目列表
│   ├── see-a-demo/               # 演示页面
│   ├── site-map/                 # 站点地图
│   ├── terms-of-service/         # 服务条款
│   ├── webhook/                  # Webhook 处理
│   ├── workflows/                # 工作流
│   ├── layout.tsx                # 根布局（RSC）
│   └── page.tsx                  # 首页
├── components/                    # 业务组件
│   ├── store/                    # MobX 状态管理
│   │   ├── editor/              # 编辑器状态（核心）
│   │   ├── create/              # 创建项目状态
│   │   ├── hosting/             # 托管状态
│   │   └── state/               # 全局状态
│   ├── tools/                    # 工具组件
│   └── ui/                       # UI 组件
├── server/                        # 服务端代码
│   └── api/                      # tRPC API
│       ├── routers/              # API 路由器
│       │   ├── chat/            # AI 聊天
│       │   ├── code.ts          # 代码操作
│       │   ├── domain/          # 域名管理
│       │   ├── forward/         # 转发服务
│       │   ├── github.ts        # GitHub 集成
│       │   ├── image.ts         # 图片处理
│       │   ├── project/         # 项目管理
│       │   ├── publish/         # 发布功能
│       │   ├── subscription/    # 订阅管理
│       │   ├── usage/           # 使用统计
│       │   └── user/            # 用户管理
│       ├── root.ts               # tRPC 根路由
│       └── trpc.ts               # tRPC 配置
├── trpc/                          # tRPC 客户端
│   ├── react.tsx                 # React Query 集成
│   └── server.ts                 # 服务端调用
├── utils/                         # 工具函数
│   └── supabase/                 # Supabase 客户端
│       ├── server.ts             # 服务端客户端
│       └── client/               # 浏览器客户端
└── env.ts                         # 环境变量验证
```

## 核心功能模块

### 1. 编辑器引擎 (EditorEngine)

位于 `apps/web/client/src/components/store/editor/engine.ts`，这是整个可视化编辑器的核心状态管理器，使用 MobX 的 `makeAutoObservable` 实现响应式。

**主要管理器**：
- **BranchManager**: Git 分支管理
- **SandboxManager**: 代码沙箱管理
- **CanvasManager**: 画布管理
- **ElementsManager**: DOM 元素管理
- **StyleManager**: 样式编辑
- **TextEditingManager**: 文本编辑
- **InsertManager**: 元素插入
- **MoveManager**: 元素移动
- **CopyManager**: 复制粘贴
- **GroupManager**: 元素分组
- **AstManager**: AST 语法树操作
- **ActionManager**: 操作历史（撤销/重做）
- **ChatManager**: AI 聊天集成
- **CodeManager**: 代码编辑
- **FontManager**: 字体管理
- **ImageManager**: 图片管理
- **ThemeManager**: 主题管理
- **IdeManager**: IDE 集成
- **FramesManager**: iframe 框架管理
- **OverlayManager**: 覆盖层管理
- **SnapManager**: 对齐吸附
- **ScreenshotManager**: 截图功能
- **PagesManager**: 页面管理

### 2. tRPC API 层

位于 `apps/web/client/src/server/api/`，提供类型安全的 API 接口。

**主要路由器**：
- `chatRouter`: AI 对话功能
- `projectRouter`: 项目 CRUD
- `branchRouter`: Git 分支操作
- `githubRouter`: GitHub 集成
- `sandboxRouter`: 代码沙箱
- `userRouter`: 用户管理
- `subscriptionRouter`: 订阅管理
- `publishRouter`: 项目发布
- `domainRouter`: 自定义域名
- `frameRouter`: iframe 管理
- `settingsRouter`: 设置管理
- `usageRouter`: 使用量统计

### 3. 数据库 Schema

使用 Drizzle ORM，schema 位于 `packages/db/src/schema/`：

**主要表**：
- `user`: 用户信息
- `project`: 项目数据
- `canvas`: 画布状态
- `chat`: AI 聊天记录
- `domain`: 自定义域名
- `subscription`: 订阅信息
- `feedback`: 用户反馈

### 4. AI 功能集成

位于 `packages/ai/`，集成多个 AI 提供商：

**支持的模型提供商**：
- OpenRouter（主要）
- Anthropic Claude
- OpenAI
- Google AI Studio
- Google Vertex AI
- AWS Bedrock

**AI 功能**：
- 代码生成
- 设计建议
- 智能对话
- 网页爬取（Firecrawl）
- 搜索增强（Exa）

### 5. 代码沙箱

位于 `packages/code-provider/`，提供代码执行环境：

- 使用 CodeSandbox SDK
- 支持 Next.js 项目
- 实时预览
- 文件系统操作

### 6. JSX/TSX 解析器

位于 `packages/parser/`，基于 Babel：

- 解析 React 组件
- AST 操作
- 代码转换
- 样式提取

## 技术特点

### 1. Next.js App Router 架构

- **服务端组件（RSC）优先**: 默认使用 Server Components
- **客户端边界明确**: 使用 `'use client'` 标记
- **tRPC 集成**: 类型安全的 API 调用
- **SuperJSON**: 自动序列化复杂数据类型

### 2. MobX 状态管理最佳实践

```typescript
// 使用 useState 创建 store 实例，保证稳定性
const [store] = useState(() => new EditorEngine(projectId, posthog));

// 使用 useRef 保存活跃 store，避免路由切换时的竞态条件
const storeRef = useRef(store);

// 异步清理，避免路由切换时的数据丢失
useEffect(() => {
    return () => {
        setTimeout(() => storeRef.current?.clear(), 0);
    };
}, []);
```

### 3. 环境变量管理

使用 `@t3-oss/env-nextjs` 进行严格的环境变量验证：

```typescript
// apps/web/client/src/env.ts
export const env = createEnv({
    server: {
        NODE_ENV: z.enum(['development', 'test', 'production']),
        SUPABASE_DATABASE_URL: z.url(),
        OPENROUTER_API_KEY: z.string(),
        // ... 更多服务端变量
    },
    client: {
        NEXT_PUBLIC_SITE_URL: z.url(),
        NEXT_PUBLIC_SUPABASE_URL: z.string(),
        // ... 更多客户端变量
    },
});
```

### 4. 国际化支持

使用 `next-intl`，消息文件位于 `apps/web/client/messages/`：

- 支持多语言
- 类型安全的翻译键
- 服务端和客户端共享

### 5. 认证与授权

- **Supabase Auth**: OAuth 集成
- **服务端客户端**: `utils/supabase/server.ts`（使用 Next.js cookies）
- **浏览器客户端**: `utils/supabase/client/index.ts`
- **严格分离**: 服务端客户端不能传递到客户端代码

## 开发工作流

### 常用命令

```bash
# 开发
bun dev                    # 启动 web client 开发服务器
bun dev:admin             # 启动管理后台

# 构建
bun build                 # 构建生产版本
bun start                 # 启动生产服务器

# 数据库
bun db:push               # 推送 schema 到数据库
bun db:migrate            # 运行迁移
bun db:seed               # 填充种子数据
bun db:studio             # 打开 Drizzle Studio

# 测试
bun test                  # 运行所有测试
bun typecheck             # 类型检查

# 代码质量
bun lint                  # 运行 linter
bun format                # 格式化代码

# Docker
bun docker:build          # 构建 Docker 镜像
bun docker:up             # 启动容器
bun docker:down           # 停止容器
```

### CI/CD

使用 GitHub Actions：

- **Typecheck**: 类型检查
- **Test**: 单元测试
- **Chromatic**: 视觉回归测试（Storybook）
- **Lint**: 代码质量检查（待启用）

## 项目规模

### 代码统计

- **总 TypeScript/TSX 文件**: ~35,777 个
- **主应用代码行数**: ~73,333 行
- **Packages 数量**: 20 个
- **Apps 数量**: 2 个（backend + web）

### 依赖关系

**主要依赖**：
- React 生态: react, react-dom, next
- 状态管理: mobx-react-lite
- API: @trpc/client, @trpc/server, @tanstack/react-query
- UI: lucide-react, motion, tailwindcss
- 编辑器: @codemirror/*, @uiw/react-codemirror
- AI: ai, openai, @openrouter/ai-sdk-provider
- 数据库: drizzle-orm, postgres
- 认证: @supabase/ssr
- 工具: zod, uuid, date-fns

## 安全性

项目已修复多个 CVE 漏洞：

- **CVE-2025-63785**: DOM XSS 漏洞（文本编辑器）
- **CVE-2025-63784**: OAuth 回调开放重定向漏洞
- **CVE-2025-63783**: 项目 mutation API 授权检查

## 商业模式

- **订阅制**: 集成 Stripe 支付
- **使用量统计**: 跟踪 API 调用
- **增长分析**: PostHog 集成
- **用户反馈**: Gleap 集成

## 部署方案

### Docker 部署

```yaml
# docker-compose.yml
services:
  web-client:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    network_mode: host
```

### Standalone 构建

```bash
bun run build:standalone
bun run start:standalone
```

支持 Vercel 部署，使用 `output: 'standalone'` 配置。

## 开发规范

### 代码组织

1. **优先使用服务端组件**: 除非需要交互、状态或浏览器 API
2. **明确客户端边界**: 在功能入口处添加 `'use client'`
3. **tRPC 路由器必须导出**: 在 `src/server/api/root.ts` 中注册
4. **环境变量必须验证**: 在 `src/env.ts` 中定义
5. **国际化文本**: 使用 `messages/*` 文件，避免硬编码
6. **MobX store 创建**: 使用 `useState(() => new Store())`
7. **路径别名**: 使用 `@/*` 和 `~/*` 映射到 `src/*`

### 禁止事项

- ❌ 不要修改生成的文件（`.next/`, `node_modules/`, lockfiles）
- ❌ 不要使用 npm/yarn/pnpm，只用 Bun
- ❌ 不要在自动化中运行 dev server
- ❌ 不要运行 `db:gen`（保留给维护者）
- ❌ 不要在客户端代码中导入服务端模块
- ❌ 不要在客户端代码中使用 `process.env`（除非有 `typeof window` 守卫）
- ❌ 不要使用 `useMemo` 创建 MobX store

## 项目亮点

### 1. 创新的可视化编辑体验

- 直接在浏览器 DOM 中编辑
- 实时预览
- Figma 风格的 UI
- 代码与设计同步

### 2. AI 驱动的开发流程

- 从文本或图片创建应用
- AI 辅助编辑
- 智能代码生成
- 多模型支持

### 3. 完整的工程化实践

- Monorepo 架构
- 类型安全的 API
- 严格的环境变量验证
- 完善的测试覆盖
- CI/CD 自动化

### 4. 现代化技术栈

- Next.js 16 App Router
- React 19
- TailwindCSS 4
- Bun 包管理器
- Drizzle ORM

### 5. 开源与社区

- Apache 2.0 协议
- 活跃的 Discord 社区
- 欢迎贡献者
- 详细的文档

## 未来规划

根据 README，计划中的功能：

- [ ] 从 Figma 导入
- [ ] 从 GitHub 仓库导入
- [ ] 向 GitHub 仓库提交 PR
- [ ] 更多 AI 功能增强
- [ ] 移动端支持
- [ ] 更多框架支持（目前仅 Next.js）

## 总结

Onlook 是一个雄心勃勃的开源项目，旨在弥合设计和开发之间的鸿沟。它结合了可视化编辑器的直观性和代码编辑器的强大功能，通过 AI 增强开发体验。项目采用现代化的技术栈和工程实践，代码质量高，架构清晰，是学习 Next.js、tRPC、MobX 和 AI 集成的优秀案例。

**适合人群**：
- 希望快速原型设计的设计师
- 需要可视化工具的前端开发者
- 学习现代 Web 开发的工程师
- 对 AI 辅助开发感兴趣的开发者

**技术亮点**：
- Next.js App Router 最佳实践
- tRPC 类型安全 API
- MobX 响应式状态管理
- Monorepo 工程化
- AI 多模型集成
- 完善的开发工具链
